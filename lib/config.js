import config from '../config/openclaw.config';

export default config;
export const {
  gateway,
  telegram,
  whatsapp,
  vault,
  googleDrive,
  projects,
  agentSystems,
  agents,
  approvalRules,
  ui,
  localAiTools,
} = config;
